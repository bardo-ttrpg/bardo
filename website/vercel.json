{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"installCommand": "if [ -f bun.lock ]; then bun install --frozen-lockfile; else cd .. && bun install --frozen-lockfile; fi",
	"buildCommand": "if [ -f bun.lock ]; then bun run --cwd website build; else cd .. && bun run --cwd website build; fi",
	"fluid": true,
	"git": {
		"deploymentEnabled": {
			"**": false,
			"main": true
		}
	}
}
