# Contributing

Thanks for helping make Bardo trustworthy and useful.

Use Node.js 24 LTS and pnpm. Keep local tooling transparent, avoid hidden network behavior, and document any file reads, writes, or generated artifacts that affect a user's campaign workspace.

Before opening a pull request, run:

```sh
pnpm check
```
