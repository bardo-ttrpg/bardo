# Security

Bardo local tooling reads only the configured workspace and its `.bardo/` directory. Report path traversal, unsafe file access, secret exposure, or unexpected network behavior as security issues.

Do not include campaign files, API keys, Clerk secrets, Convex deployment secrets, or hosted app credentials in issues.
