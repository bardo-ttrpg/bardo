# Publishing

Public release targets include npm packages, MCP registry metadata, Agent Skills distribution, and client marketplace listings.

The public repo must not include SaaS secrets, private app code, Clerk configuration secrets, Convex deployment credentials, or paid-entitlement bypasses.
